import ClientTemplate from "../components/ClientTemplate/ClientTemplate";

function LinkedIn() {
  return (
    <ClientTemplate>
      LinkedIn is a professional networking platform. People use it to find jobs
      and build business connections.
    </ClientTemplate>
  );
}

export default LinkedIn;
